# Tail dispositions

### ADOPT-27
**Disposition:** RESOLVED -- audit_round_format is now wired in algopacksimple, claude-usage-notifier and flutter_uptime_monitor. Each repo gets `audits/README.md` (rounds go in `audits/<yyyy-mm-dd>/`, closed rounds in `audits/implemented/`, with the shared heading, disposition and tracker format), `audits/TRACKER.md` (a cross-round index table with a `Disposition` column and no rows yet), and a meta test `test_audit_rounds_are_countable.py`. The test has three checks. (1) The README exists, `tracker_status_cells(audits/TRACKER.md)` is not None (the parser found the table), and the index has one row per dated round directory. (2) `finding_problems` holds over every `round_files` file, and a round with no disposition fails. (3) `assert_rounds_filed(AUDITS, min_trackers=<number of dated round dirs>)`. An empty tree that is still valid passes through the gate's own documented `min_trackers` parameter, and the floor rises with the first round, so this is not a py-ci-shared defect and no t2_upstream.md was written. Teeth checked in the notifier with a scratch round that was deleted afterwards. A round with an undispositioned finding and no tracker failed all 3 tests. A closed round left in the open tree failed with "every row of TRACKER.md is closed - move the round to implemented/". The same round filed under implemented/ gave 3 passed. Notifier: it had no tests dir, so it gets `tests/` with this one test, `pytest>=8.0` in its dev extra, and a new `meta-tests` CI job. That job pip-installs only the dev extra, read from pyproject with tomllib, because the repo is a flat script set with no build backend. The test never imports `config`, so no `.env` secrets are needed. algopacksimple: the test is added to CI's explicit pytest file list. flutter_uptime_monitor has no CI and no pre-commit, so its test only runs locally. Verified: t2_1_algopacksimple.log, t2_1_claude-usage-notifier.log and t2_1_flutter_uptime_monitor.log each show 3 passed. Teeth logs: t2_1_teeth_{a,b,c}.log. actionlint is clean on both edited workflows. Pushed: algopacksimple f41a7b1, claude-usage-notifier 402c4be, flutter_uptime_monitor 63f186b.

### ADOPT-28
**Disposition:** RESOLVED -- social's production_scrapers and dashboard now both call the shared `lint-blocking.yml` at the v1.17.0 pin, with `working-directory` set to the subproject. No workflow input was missing, so no upstream proposal was needed. - **production_scrapers** (`production-scrapers-ci.yml`, job `lint-blocking`): - Inputs: bandit `-c pyproject.toml` with `-x ./tests,./probes,./scripts` (the triaged `[tool.bandit]` skips, same as its blocking hook), vulture with `scripts/vulture_whitelist.py`, codespell `--toml pyproject.toml .`, interrogate at 35, deptry via `pip install -r requirements.txt`, and `run-zizmor: true` (`zizmor .` at the repo root reports no findings). - Vulture policy: the baseline script's exclude globs moved into a new `[tool.vulture]` table, so there is one copy. `scripts/run_vulture_check.py` now omits `--exclude` and reads the same table. The bundle command `vulture . scripts/vulture_whitelist.py --min-confidence 80` and the hook both report 0 (was 54 raw). - Also fixed: a docstring in `scripts/recover_uid_for_cipher_rows.py:99` used an invalid escape (`\``), which gives a SyntaxWarning on every scan. It is now a raw docstring. - **dashboard** (`dashboard-ci.yml`, job `lint-blocking`): - Bandit: 9 B608 findings remain today, not 14. I reviewed each site: every one interpolates only module constants, literal CTEs or `_OUTER_ORDERINGS` entries, and all values go through `%(name)s` binds. Each site now carries its own `# nosec B608 - <what is interpolated>`. There is no blanket skip and no `[tool.bandit]` skip list. - For the multi-line f-strings the marker sits on the closing `"""` line. On the opening line it would have been SQL string content. - Under the repo's own rule ("a hook goes blocking when its number reaches zero"), the `bandit-warn || true` pre-commit hook became `bandit-blocking`. - Inputs: `bandit-exclude ./tests`, vulture with its whitelist, interrogate at 50, codespell toml. deptry stays off because the folder declares no dependencies, matching its existing hooks. - **Local run of the bundle's exact commands:** - production_scrapers: bandit 0, vulture 0, codespell 0, interrogate 46.4% >= 35, deptry "No dependency issues". - dashboard: bandit 0 (rc 0), vulture 0, codespell 0, interrogate 51.2% >= 50. - Repo root: actionlint clean, yamllint clean (apart from CRLF new-lines in autocrlf working copies), zizmor 0. - **Targeted tests:** dashboard 373 passed (t2_2_dashboard.log), production_scrapers meta 47 passed (t2_2_ps.log). Pushed: social a4574716.

### ADOPT-24
**Disposition:** DEFERRED -- the pair-by-pair mapping of pyutilz dev/code_audit scanners to py-ci-shared gates is recorded (ad2.md), and the consolidation (delegate where the gate provably covers the scanner without re-keying consumer baselines, otherwise cross-reference and upstream the missing shapes) was started but interrupted by the API weekly limit; its partial, uncommitted edits are in a pyutilz worktree. Waiting on: that resumed run (after 2026-10-01), then a baseline re-key check across the code_audit_meta consumers (pyutilz, mlframe, social, llm_bench, algopacksimple, autopsia, glossum, dash_app_core).
