from sqlalchemy.ext.asyncio import create_async_engine


async def seed_main():
    eng = create_async_engine("sqlite://")
    try:
        await eng.connect()
    finally:
        await eng.dispose()
