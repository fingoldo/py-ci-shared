import threading

_SEED_LOCK = threading.Lock()
_SEED_CACHE = {}


def seed_get(k):
    with _SEED_LOCK:
        _SEED_CACHE[k] = 1
        if len(_SEED_CACHE) > 10:
            _SEED_CACHE.popitem()
