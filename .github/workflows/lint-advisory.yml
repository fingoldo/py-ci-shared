name: Lint (advisory bundle, reusable)

# Every step here is non-blocking (continue-on-error) because it's inherently a design-taste
# signal rather than a correctness bug (mccabe complexity), or a CVE feed whose fix isn't always
# on your own timeline (pip-audit). Checks that CAN reach a clean 0-findings state after a real
# triage pass (bandit, vulture, codespell, yamllint, interrogate, deptry) live in
# lint-blocking.yml instead once triaged. Real-bug gates also live in ruff-blocking.yml /
# black-filtered.yml / mypy-beachhead.yml.

on:
  workflow_call:
    inputs:
      source-path:
        description: 'Path to the package source, e.g. "src/mlframe"'
        required: true
        type: string

jobs:
  lint-advisory:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: "3.11"

      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86  # v5.4.2
        with:
          enable-cache: true

      # Same requirement as ruff-blocking.yml: the calling repo's pyproject.toml does
      # `[tool.ruff] extend = "../py-ci-shared/configs/ruff-base.toml"`, a real filesystem path.
      # Missing this step made both ruff steps below fail at config-load time on every run,
      # silently swallowed by continue-on-error -- found 2026-07-09 in a CI/CD architecture
      # review, an unknown amount of time after the reusable-workflow split.
      - name: Clone py-ci-shared (sibling, for ruff `extend` path resolution)
        run: git clone --depth 1 https://github.com/fingoldo/py-ci-shared.git ../py-ci-shared

      - name: Run ruff (full - advisory)
        run: uvx ruff check "${INPUTS_SOURCE_PATH}"
        continue-on-error: true
        env:
          INPUTS_SOURCE_PATH: ${{ inputs.source-path }}

      - name: Ruff mccabe complexity (advisory)
        run: uvx ruff check "${INPUTS_SOURCE_PATH}" --select C90 --statistics
        continue-on-error: true
        env:
          INPUTS_SOURCE_PATH: ${{ inputs.source-path }}

      - name: pip-audit dependency vulnerability scan
        run: uvx pip-audit --desc -f json -o pip-audit-report.json || true
        continue-on-error: true

      - name: Upload pip-audit results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: pip-audit-report
          path: pip-audit-report.json
